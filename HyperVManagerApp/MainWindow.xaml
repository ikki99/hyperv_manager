<Window
    x:Class="HyperVManager.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <NavigationView x:Name="NavigationViewControl" 
                        Grid.Row="1" 
                        IsSettingsVisible="False" 
                        PaneDisplayMode="LeftCompact" 
                        SelectionChanged="NavigationViewControl_SelectionChanged">
            <NavigationView.MenuItems>
                <NavigationViewItem Content="虚拟机" Tag="VMs"/>
                <NavigationViewItem Content="网络设置" Tag="Network"/>
                <NavigationViewItem Content="创建虚拟机" Tag="CreateVM"/>
                <NavigationViewItem Content="系统镜像" Tag="Images"/>
                <NavigationViewItem Content="系统检查" Tag="SystemCheck"/>
            </NavigationView.MenuItems>
            <Frame x:Name="ContentFrame"/>
        </NavigationView>
    </Grid>
</Window>